Apply this learning mode while keeping the core task unchanged:
{mode_requirements}

Apply these learning-focus requirements while keeping the core task unchanged:
{focus_requirements}

Use the source text below as the full context for the job.
If the text contains a title, preserve it in the card front when useful.
If the text contains a video URL, preserve it in the card back when useful.

Source text:
{source_text}
